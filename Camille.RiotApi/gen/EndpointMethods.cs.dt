{{
    const spec = require(it.path + '/.spec.json');
    const dotUtils = require('./dotUtils.js');
    const useMethod = ([ route, path ]) => path.get && path.get.responses['200'];
}}
{{= it.templates.endpointMethods({
    spec, useMethod, namespace: 'RiotApi'
}) }}
